.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri
/target
/src-tauri/target
/src-tauri/gen/schemas

# Updater signing key — NEVER commit (free self-generated key; pubkey lives in tauri.conf.json)
/.tauri-updater.key
/.tauri-updater.key.pub

# comfysort debug logs
*.log

# Local-only docs (internal architecture / design / handoff / agent workflow +
# context for cross-session iteration) and local Claude tooling. Kept out of the
# public repo; README.md (public) lives at the root.
# Exception: docs/screenshots/ holds the README hero + screenshots (published).
/docs/*
!/docs/screenshots/
/.claude/

# Agent/contributor entry (local-only; harness-neutral front door)
/AGENTS.md

# Private build/release toolchain (not for public consumption)
/scripts/
